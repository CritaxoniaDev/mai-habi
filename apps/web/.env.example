# Origin the app is served from. Used for share links.
PUBLIC_APP_ORIGIN=http://localhost:4321

# Optional. Without these the playground runs in guest-only mode:
# projects stay in IndexedDB and share links carry their payload inline.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
